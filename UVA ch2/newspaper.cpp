#include <bits/stdc++.h>
#include <cstring>



using namespace std;

#define CHAR_OFFSET 128

//direct mapping of a[i] to b[i]
char a[100];
char b[100]; 

int main(int argc, char const *argv[])
{
    int n, m;
    cin >> TC;
    while(n--){
        cin >> m;
        int out;
        for(int i = 0; i < m; i++){
            cin >> a[i] >> b[i]; //direct mapped            
        }
        
        int lines;
        cin >> lines;
        for()
    }
    return 0;
}